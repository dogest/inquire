export enum EnumApiStatus {
  fetching = 'fetching',
  success = 'success',
  fail = 'fail',
}

export enum EnumStorageKey {
  token =  'token',
  tokenExpires = 'tokenExpires',
  userId = 'userId',
  password = 'password',
  userInfo = 'userInfo',
}
